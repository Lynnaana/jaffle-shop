snapshots:
    - name: customers_snapshot
      relation: source('ecom','customers')
      config:
        schema: snapshots
        database: analytics
        unique_key: id
        strategy: check
        check_cols: ['id', 'name']
        hard_deletes: ignore
        dbt_valid_to_current: "to_date('9999-12-31')"